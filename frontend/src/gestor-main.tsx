import React from 'react'
import ReactDOM from 'react-dom/client'
import { GestorPortalApp } from './GestorPortalApp'

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
  <React.StrictMode>
    <GestorPortalApp />
  </React.StrictMode>,
)

